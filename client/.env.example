# For local dev leave empty; Vite will proxy /api to http://localhost:3001
# For production, set this to your deployed server URL, e.g.:
# VITE_API_BASE=https://infohub-server.onrender.com
VITE_API_BASE=
